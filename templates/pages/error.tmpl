{{ define "title-pages/error" }}{{ .status }} | Eating is Activism{{ end }}
{{ define "description-pages/error"}}{{ .message }}{{ end }}
{{ define "mainClass-pages/error" }}flex flex-col{{ end }}

<section class="container mx-auto h-full flex flex-col justify-center grow">
  <h1 class="text-center font-bold text-9xl mb-10">{{ .status }}</h1>
  <p class="text-center">{{ .message }}</p>
</section>